# 아이템 조합 분석기

## 개요

1라운드에서 받은 조합 아이템으로 어떤 완성 아이템을 만드는 것이 승률이 높은지 통계로 분석

## 핵심 로직

### 입력
- 조합 아이템 선택 (**최대 4개**, 중복 가능)
- 예: 대검, 곡궁, 갑옷, 망토

> **4개 제한 이유**: 1라운드에서 일반적으로 받는 조합템 개수 기준

### 처리 과정

#### 1단계: 가능한 완성 아이템 조합 계산

선택한 조합 아이템으로 만들 수 있는 완성 아이템의 **모든 경우의 수** 계산

```
입력: 대검, 곡궁, 갑옷

가능한 조합:
1. 거인학살자(대검+곡궁) + 갑옷 남음
2. 수호천사(대검+갑옷) + 곡궁 남음
3. 유령무희(곡궁+갑옷) + 대검 남음
```

#### 2단계: 각 조합별 통계 계산 (남은 아이템 기반 분류)

**핵심**: 같은 완성 아이템이라도 **남은 조합 아이템**에 따라 별도 통계로 분류

```
예시: 구인수의 격노검을 만들 수 있는 경우

입력 1: 곡궁, 장갑, 갑옷
→ 구인수(곡궁+장갑) + 갑옷 남음 → 평균 4.2등

입력 2: 곡궁, 장갑, 대검
→ 구인수(곡궁+장갑) + 대검 남음 → 평균 3.8등
```

**이유**: 남은 아이템에 따라 다음에 만들 수 있는 아이템이 달라지므로 플레이 방향과 승률이 달라짐

DB에서 해당 완성 아이템 + 남은 조합템 조건을 만족하는 게임들을 필터링하여 통계 계산

#### 3단계: 상세 게임 정보 제공

각 조합별로 **최대 10게임**의 상세 정보 표시:
- 덱 구성 (챔피언 목록)
- 전체 아이템 조합
- 최종 순위

### 출력 형태

```
분석 결과 (총 120게임 분석)

#1 거인학살자(대검+곡궁) + 갑옷 남음
   평균 3.2등 | 상위4 72% | 45게임

   [상세 보기]
   ├─ 1등: 진, 자야, 트위치... | 거인학살자, 구인수, 피바라기
   ├─ 2등: 녹턴, 렝가... | 거인학살자, 무한의대검, 최속
   └─ ... (최대 10게임)

#2 수호천사(대검+갑옷) + 곡궁 남음
   평균 3.8등 | 상위4 65% | 38게임
   ...

#3 유령무희(곡궁+갑옷) + 대검 남음
   평균 4.1등 | 상위4 58% | 37게임
   ...
```

### 통계 필터링 로직

```
조건 1: 완성 아이템 보유
- 플레이어가 해당 완성 아이템을 보유했는지 확인

조건 2: 남은 조합 아이템으로 만들 수 있는 완성 아이템 보유 (선택적)
- 남은 조합템이 있는 경우, 그 조합템으로 만들 수 있는
  완성 아이템 중 하나라도 보유했는지 확인
- 이를 통해 "비슷한 빌드 경로"의 게임만 필터링

예시:
입력: 대검, 곡궁, 갑옷
조합: 거인학살자(대검+곡궁) + 갑옷 남음

필터링 조건:
1. 거인학살자 보유 AND
2. (갑옷으로 만들 수 있는 아이템 중 하나 보유)
   - 수호천사, 선파이어, 가시갑옷, 적응형, 승천의 갑옷 등
```

## 데이터 수집

### 수집 방식

```
챌린저 상위 200명
    ↓
각 플레이어의 최근 20게임 수집 (Riot API)
    ↓
1게임 = 8명 플레이어 데이터 저장
```

### 데이터 규모

| 항목 | 설명 |
|------|------|
| tft_matches | 매치 정보 (1게임 = 1레코드) |
| tft_players | 플레이어 정보 (1게임 = 8레코드) |
| 총 레코드 | 약 16,000~20,000개 (플레이어 데이터) |
| 실제 매치 수 | 레코드 수 ÷ 8 |

> **참고**: UI에 표시되는 "X개 플레이어 데이터 기반"은 `tft_players` 테이블의 레코드 수입니다.

## 데이터 구조

### DB 테이블: tft_players

```sql
- placement: 최종 순위 (1~8)
- units: JSON 배열
  - character_id: 챔피언 ID
  - itemNames: 완성 아이템 API 이름 배열
  - tier: 성급
```

### 아이템 매핑

- 조합 아이템: "BFSword", "RecurveBow", "ChainVest" 등 (문자열)
- 완성 아이템: "TFT_Item_MadredsBloodrazor" 등 (API 이름)
- 조합 레시피: ITEM_RECIPES 객체에 정의

## API 설계

### POST /api/stats/item-analysis

**Request:**
```json
{
  "components": ["BFSword", "RecurveBow", "ChainVest"]
}
```

**Response:**
```json
{
  "totalRecords": 16509,
  "inputComponents": ["BFSword", "RecurveBow", "ChainVest"],
  "inputComponentNames": ["대검", "활", "갑옷"],
  "combinations": [
    {
      "mainItem": "TFT_Item_MadredsBloodrazor",
      "mainItemName": "거인학살자",
      "secondItem": "TFT_Item_xxx",        // 4개 선택 시 두 번째 완성 아이템
      "secondItemName": "xxx",
      "usedComponents": ["BFSword", "RecurveBow"],
      "remainingComponents": ["ChainVest"],
      "remainingComponentNames": ["갑옷"],
      "avgPlacement": 3.2,
      "gameCount": 45,
      "topFourRate": 72,
      "sampleGames": [
        {
          "placement": 1,
          "units": [
            {
              "apiName": "TFT16_Jinx",
              "name": "Jinx",
              "items": ["TFT_Item_xxx", ...],
              "itemNames": ["아이템명", ...]
            },
            ...
          ]
        },
        ...
      ]
    },
    ...
  ]
}
```

## 파일 구조

```
src/
├── data/
│   └── item-components.ts     # 아이템 조합 레시피 데이터
├── app/
│   ├── api/stats/item-analysis/
│   │   └── route.ts           # 통계 계산 API
│   └── simulator/item-analysis/
│       └── page.tsx           # UI 페이지
└── scripts/
    └── collect-tft-data.js    # 데이터 수집 스크립트
```

## 아이템 이미지 표시

### 이미지 소스

**Data Dragon (Riot 공식 CDN)** 사용

```
URL 패턴:
https://ddragon.leagueoflegends.com/cdn/{version}/img/tft-item/{itemApiName}.png

예시:
- 대검: https://ddragon.leagueoflegends.com/cdn/15.24.1/img/tft-item/TFT_Item_BFSword.png
- 무한의 대검: https://ddragon.leagueoflegends.com/cdn/15.24.1/img/tft-item/TFT_Item_InfinityEdge.png
- Set16 엠블럼: https://ddragon.leagueoflegends.com/cdn/15.24.1/img/tft-item/TFT16_Item_BilgewaterEmblemItem.png
```

### 적용 위치

1. **조합 아이템 선택 버튼**: 이모지 → 아이템 이미지
2. **선택된 아이템 표시**: 텍스트 옆에 아이템 이미지
3. **결과 카드**: 완성 아이템 이름 옆에 이미지
4. **남은 조합 아이템**: 조합템 이미지 표시
5. **샘플 게임 아이템 목록**: 아이템 이미지로 표시

### 이미지 URL 생성

```typescript
// src/lib/itemImage.ts

const DDRAGON_VERSION = "15.24.1";
const DDRAGON_BASE = "https://ddragon.leagueoflegends.com/cdn";

// 완성 아이템 이미지 (TFT_Item_xxx 형태)
export function getItemImageUrl(itemApiName: string): string {
  return `${DDRAGON_BASE}/${DDRAGON_VERSION}/img/tft-item/${itemApiName}.png`;
}

// 조합 아이템 이미지 (BFSword → TFT_Item_BFSword)
export function getComponentImageUrl(componentId: string): string {
  return `${DDRAGON_BASE}/${DDRAGON_VERSION}/img/tft-item/TFT_Item_${componentId}.png`;
}
```

### UI 스펙

| 위치 | 이미지 크기 | 추가 스타일 |
|------|------------|------------|
| 조합 아이템 버튼 | 32x32 | - |
| 선택된 아이템 | 24x24 | rounded |
| 결과 카드 메인 아이템 | 40x40 | border, shadow |
| 남은 조합 아이템 | 24x24 | opacity-80 |
| 샘플 게임 아이템 | 20x20 | rounded |

## 구현 상태

- [x] 데이터 수집 (664개 게임 → 200명 확장 중)
- [x] 기본 UI
- [x] 경우의 수 기반 로직
- [x] 상세 게임 정보 표시
- [x] 덱 구성 표시
- [x] API 타임아웃 해결 (페이지네이션 + 캐싱)
- [x] 통계 편향 해결 (아이템 개수 기반 가중치)
- [x] 조합 아이템 최대 4개 제한
- [x] 남은 아이템 기반 통계 분류
- [x] 아이템 이미지 표시 (Data Dragon CDN)

---

## v2 개선 요구사항 (2024-12-28)

### 요구사항 목록

| # | 요구사항 | 설명 | 우선순위 |
|---|----------|------|----------|
| 1 | 최소 선택 개수 변경 | 1개 → **2개** (초반에 최소 2개는 얻음) | 높음 |
| 2 | 완성아이템 2개 조합 통계 | 4개 선택 시 "아이템A + 아이템B" 통계 추가 | 높음 |
| 3 | 샘플 게임 정렬 변경 | 1등만 → **최신 10게임** (등수 다양하게) | 중간 |
| 4 | 챔피언 이미지 표시 | 텍스트 → 이미지로 변경 | 중간 |
| 5 | 챔피언별 아이템 표시 | 챔피언 이미지 하단에 장착 아이템 표시 | 중간 |
| 6 | 타겟 아이템 챔피언 하이라이트 | 해당 아이템 장착 챔피언 강조 표시 | 낮음 |

---

### 요구사항 1: 최소 선택 개수 변경

**변경 전**: 최소 1개 ~ 최대 4개
**변경 후**: 최소 2개 ~ 최대 4개

**이유**: 1라운드 초반에 최소 2개의 조합 아이템은 얻기 때문

**영향 범위**:
- UI: 분석 버튼 disabled 조건 (1개 → 2개)
- UI: 안내 문구 수정

---

### 요구사항 2: 완성아이템 2개 조합 통계

**현재 동작** (4개 선택 시):
```
입력: 대검, 지팡이, 벨트, 눈물

결과:
- 마법공학 총검(대검+지팡이) + [벨트, 눈물] 남음
- 스테락의 도전(대검+벨트) + [지팡이, 눈물] 남음
- 대천사의 지팡이(지팡이+눈물) + [대검, 벨트] 남음
```

**추가할 동작**:
```
입력: 대검, 지팡이, 벨트, 눈물

결과 (기존 + 추가):
- 마법공학 총검 + [벨트, 눈물] 남음
- **마법공학 총검 + 정령의 형상** ← 추가 (벨트+눈물로 완성)
- 스테락의 도전 + [지팡이, 눈물] 남음
- **스테락의 도전 + 대천사의 지팡이** ← 추가 (지팡이+눈물로 완성)
- 대천사의 지팡이 + [대검, 벨트] 남음
- **대천사의 지팡이 + 스테락의 도전** ← 추가 (대검+벨트로 완성)
```

**로직**:
1. 남은 조합 아이템이 정확히 2개인 경우
2. 그 2개로 만들 수 있는 완성 아이템이 있으면
3. "첫번째 아이템 + 두번째 아이템" 조합으로 별도 통계 생성

**API 응답 변경**:
```typescript
interface CombinationResult {
  mainItem: string;
  mainItemName: string;
  secondItem?: string;        // 추가: 두 번째 완성 아이템
  secondItemName?: string;    // 추가
  usedComponents: string[];
  remainingComponents: string[];  // 2개 아이템 조합이면 빈 배열
  // ...
}
```

---

### 요구사항 3: 샘플 게임 정렬 변경

**변경 전**: 1등 게임만 10개 표시
**변경 후**: 최신 10게임 (등수 다양하게)

**이유**: 1등만 보면 편향됨. 실제로 1등~8등 다양하게 나오는 것을 보여줘야 함

**영향 범위**:
- API: ORDER BY 변경 (placement → created_at DESC 또는 match_id DESC)

---

### 요구사항 4: 챔피언 이미지 표시

**변경 전**: 텍스트 ("Jinx, Zaya, Twitch...")
**변경 후**: 이미지 (48x48 챔피언 초상화)

**이미지 소스**: 로컬 저장된 챔피언 이미지 (`public/images/champions/`)

**참고**: 브론즈/세계룬 시뮬레이터에서 이미 구현된 `getChampionImagePath()` 활용

---

### 요구사항 5: 챔피언별 아이템 표시

**UI 목업**:
```
┌──────┐ ┌──────┐ ┌──────┐
│ 진   │ │브라이어│ │ 세주 │
│ 48x48│ │ 48x48 │ │ 48x48│
├──────┤ ├──────┤ ├──────┤
│🗡️🛡️🔮│ │🗡️🗡️🗡️│ │🛡️🛡️  │  ← 20x20 아이템 이미지
└──────┘ └──────┘ └──────┘
```

**데이터 구조**:
- DB의 `units` 필드에 각 챔피언별 `itemNames` 배열 존재
- 이미 데이터는 있음, UI만 변경 필요

---

### 요구사항 6: 타겟 아이템 챔피언 하이라이트

**시나리오**: "쇼진의 창" 통계 → 샘플 게임에서 쇼진의 창을 착용한 챔피언 강조

**UI**:
- 테두리 색상 변경 (노란색 또는 파란색)
- 또는 배경 하이라이트

---

## 구현 순서

### Phase 1: 기본 기능 개선 (API 중심)

| 순서 | 작업 | 예상 난이도 | 테스트 방법 |
|------|------|------------|------------|
| 1-1 | 최소 선택 2개 제한 | 쉬움 | UI에서 1개만 선택 후 버튼 disabled 확인 |
| 1-2 | 샘플 게임 최신순 정렬 | 쉬움 | API 응답에서 placement가 다양한지 확인 |
| 1-3 | 완성아이템 2개 조합 통계 | 중간 | 4개 선택 시 2개 아이템 조합 결과 확인 |

### Phase 2: UI 개선 (프론트엔드 중심)

| 순서 | 작업 | 예상 난이도 | 테스트 방법 |
|------|------|------------|------------|
| 2-1 | 챔피언 이미지 표시 | 쉬움 | 샘플 게임에서 이미지 렌더링 확인 |
| 2-2 | 챔피언별 아이템 표시 | 중간 | 각 챔피언 하단에 아이템 이미지 확인 |
| 2-3 | 타겟 아이템 하이라이트 | 쉬움 | 해당 아이템 착용 챔피언 강조 확인 |

---

## 테스트 프로세스

### 단계별 테스트 체크리스트

```
┌─────────────────────────────────────────────────────────────┐
│  1. 구현 전: 현재 상태 스크린샷 저장                          │
├─────────────────────────────────────────────────────────────┤
│  □ 변경 전 UI 스크린샷 (비교용)                              │
│  □ 변경 전 API 응답 샘플 저장                                │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  2. 구현 중: 단위 테스트                                     │
├─────────────────────────────────────────────────────────────┤
│  □ 콘솔 에러 없음 확인                                       │
│  □ 빌드 에러 없음 확인 (npm run build)                       │
│  □ TypeScript 타입 에러 없음                                 │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  3. 구현 후: 통합 테스트                                     │
├─────────────────────────────────────────────────────────────┤
│  □ 기능별 테스트 케이스 실행 (아래 참조)                     │
│  □ 기존 기능 회귀 테스트 (기존 동작 유지 확인)               │
│  □ 브라우저 콘솔에 에러 없음                                 │
│  □ 네트워크 탭에서 실패한 요청 없음                          │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│  4. 완료 보고                                                │
├─────────────────────────────────────────────────────────────┤
│  □ "✅ 테스트 완료" + 스크린샷/결과 공유                     │
│  □ 문서 구현 상태 체크박스 업데이트                          │
└─────────────────────────────────────────────────────────────┘
```

### 기능별 테스트 케이스

#### TC-1: 최소 선택 2개 제한
```
1. 조합 아이템 0개 선택 → 분석 버튼 disabled ✓
2. 조합 아이템 1개 선택 → 분석 버튼 disabled ✓ (신규)
3. 조합 아이템 2개 선택 → 분석 버튼 enabled ✓
4. 조합 아이템 4개 선택 → 분석 버튼 enabled, 추가 선택 불가 ✓
```

#### TC-2: 샘플 게임 최신순
```
1. 아무 조합 분석 실행
2. 샘플 게임 10개 확인
3. placement가 1등만 아닌지 확인 (1~8등 다양하게)
```

#### TC-3: 완성아이템 2개 조합
```
1. 대검, 지팡이, 벨트, 눈물 선택
2. 분석 실행
3. 결과에 "마법공학 총검 + 정령의 형상" 등 2개 조합 존재 확인
4. 해당 조합의 통계 (평균 등수, 게임 수) 표시 확인
```

#### TC-4: 챔피언 이미지
```
1. 아무 조합 분석 실행
2. 샘플 게임 펼치기
3. 챔피언이 이미지로 표시되는지 확인
4. 이미지 깨짐 없는지 확인
```

#### TC-5: 챔피언별 아이템
```
1. 샘플 게임에서 각 챔피언 하단에 아이템 이미지 표시 확인
2. 아이템 개수가 챔피언별로 다르게 표시되는지 확인 (0~3개)
```

#### TC-6: 타겟 아이템 하이라이트
```
1. "쇼진의 창" 조합 분석
2. 샘플 게임에서 쇼진의 창 착용 챔피언 강조 표시 확인
3. 다른 챔피언은 강조 없음 확인
```

---

## v2 구현 상태

- [x] 최소 선택 2개 제한
- [x] 완성아이템 2개 조합 통계
- [x] 샘플 게임 최신순 정렬
- [x] 챔피언 이미지 표시
- [x] 챔피언별 아이템 표시
- [x] 타겟 아이템 하이라이트

---

## 참고사항

- 챌린저 티어 데이터 기반
- Development API Key 사용 (24시간마다 재발급 필요)
- 패치마다 데이터 재수집 권장
- 최대 4개 제한: 1라운드 조합템 기준
- 아이템 이미지: Data Dragon CDN 사용 (외부 URL, 로컬 저장 불필요)
